---
phase: 01-toolchain-reality-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/pyproject.toml
  - agent/.env.example
  - agent/src/steward/__init__.py
  - agent/src/steward/chain.py
  - agent/.venv/
  - secrets/
  - deployments/testnet.json
  - .planning/phases/01-toolchain-reality-check/CLI-FLAGS.md
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
must_haves:
  truths:
    - "casper-client --version prints a 5.x version on PATH (Windows)"
    - "put-txn transfer/delegate real flag names + the JSON key holding the transaction hash are recorded from --help on the installed binary (not doc guesses)"
    - "agent/ is a runnable Python package: .venv built via py launcher, httpx + python-dotenv installed, pycspr explicitly NOT installed"
    - "chain.py exposes get_balance / transfer / confirm + a delegate stub, using flags confirmed from --help, importable without error"
    - "a fresh ed25519 keypair exists in gitignored ./secrets/, and its public_key_hex + account hash are recorded in deployments/testnet.json"
  artifacts:
    - "agent/pyproject.toml"
    - "agent/.env.example"
    - "agent/src/steward/chain.py"
    - "secrets/secret_key.pem (gitignored), secrets/public_key.pem, secrets/public_key_hex"
    - "deployments/testnet.json"
    - ".planning/phases/01-toolchain-reality-check/CLI-FLAGS.md"
  key_links:
    - "chain.py flag strings === the exact flags printed by put-txn transfer --help (RESEARCH open-risk #1)"
    - "AGENT_SECRET_KEY_PATH in .env.example -> ./secrets/secret_key.pem produced by keygen"
    - "deployments/testnet.json public_key_hex === secrets/public_key_hex (same account funded in 01-02)"
---

<objective>
Stand up the agent's chain layer against the live Casper 2.0 "Condor" testnet and lock it to REAL casper-client v5 flags — everything downstream depends on these, not on doc guesses. Install + verify casper-client v5, scaffold the `agent/` Python package (py launcher 3.14, httpx + python-dotenv, explicitly NOT pycspr), build the reusable `chain.py` wrapper, and generate + record the agent's testnet keypair.

The chain-layer decision is already made and live-verified (per 01-RESEARCH.md): drive `casper-client` v5 CLI from Python via subprocess (D-03 fallback #1; pycspr 1.2.0 is Deploy-only and cannot do Casper 2.0 native-auction delegation). This plan does NOT re-research — it implements the recipe in 01-RESEARCH.md.

Purpose: Resolve RESEARCH open-risks #1 (exact CLI flags + tx-hash JSON key) and #4 (query_balance purse-identifier shape) against the installed binary and a live RPC call, then bake the verified shapes into a chain layer that Phases 3-4 reuse unchanged (D-04).
Output: A funded-ready agent package — installed v5 client, runnable venv, importable chain.py, gitignored keypair, and deployments/testnet.json holding the agent public key + account hash. Funding + the acceptance probe happen in 01-02 (after the manual faucet gate).
</objective>

<execution_context>
@/tmp/steward/.claude/gsd-core/workflows/execute-plan.md
@/tmp/steward/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-toolchain-reality-check/01-RESEARCH.md
@.planning/phases/01-toolchain-reality-check/01-CONTEXT.md

# Environment: Windows 11, PowerShell. Python via the `py` launcher -> 3.14.2. Rust/cargo 1.95 present (D-05).
# casper-client build (cargo install casper-client --locked) is ALREADY RUNNING in the background — verify it finished, do not kick off a second build unless the first failed.
# .gitignore already covers: .env, secrets/, *.pem, __pycache__/, target/ — do NOT commit any secret key or .env.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify casper-client v5 on PATH and lock the REAL put-txn flags (TOOL-01, open-risk #1)</name>
  <files>.planning/phases/01-toolchain-reality-check/CLI-FLAGS.md</files>
  <action>
    Confirm the background `cargo install casper-client --locked` finished and the binary resolves on PATH for subprocess. Run `casper-client --version` and require it prints a 5.x version (target 5.0.1 per RESEARCH). If the binary is missing or the cargo build failed on Windows (missing C++ build tools / OpenSSL), fall back to a prebuilt casper-client-rs GitHub release binary, put it on PATH, then re-verify `--version`.

    Then resolve RESEARCH open-risk #1 by capturing the EXACT interface of the installed binary — do not trust doc spellings. Run and record the full output of: `casper-client put-txn --help`, `casper-client put-txn transfer --help`, `casper-client put-txn delegate --help`, and `casper-client get-transaction --help`. From that output, record into CLI-FLAGS.md: (a) whether the subcommand is `put-txn` or `put-transaction`; (b) the exact transfer-amount flag (`--transfer-amount` vs `--transaction-amount` vs `--amount`); (c) whether a payment/standard-payment flag is required and the exact `--pricing-mode` value; (d) the target / secret-key / chain-name / node flag spellings; (e) the delegate flags (delegator / validator / amount, plus the redelegate new-validator flag) for Phase 4; (f) the exact JSON path to the returned transaction hash in `--output json` (or `-v`) output, for example result.transaction_hash. CLI-FLAGS.md is the single source of truth that Task 3 (chain.py) copies flags from — this is a tracked .planning artifact (NOT gitignored) so the verified interface survives into Phases 3-4.
  </action>
  <verify>
    <automated>casper-client --version 2>&1 | grep -E "casper-client 5\." && casper-client put-txn transfer --help 2>&1 | grep -Eqi "secret-key" && test -f .planning/phases/01-toolchain-reality-check/CLI-FLAGS.md && grep -Eqi "transfer-amount|transaction-amount" .planning/phases/01-toolchain-reality-check/CLI-FLAGS.md</automated>
  </verify>
  <done>casper-client --version prints 5.x; put-txn transfer/delegate/get-transaction --help captured; CLI-FLAGS.md records the exact transfer flags, delegate flags, and the JSON key holding the transaction hash, sourced from the installed binary.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the agent/ Python package (py launcher venv, httpx + python-dotenv, NOT pycspr) + .env.example (TOOL-02 setup)</name>
  <files>agent/pyproject.toml, agent/.env.example, agent/src/steward/__init__.py, agent/.venv/</files>
  <action>
    Create the `agent/` package per the RESEARCH "Recommended Project Structure". Write `agent/pyproject.toml` declaring package name `steward`, `requires-python = ">=3.12"`, and runtime deps `httpx` and `python-dotenv` (src layout: package at `agent/src/steward`). Create `agent/src/steward/__init__.py` (empty or a version string).

    Build the venv with the Windows `py` launcher and install ONLY httpx + python-dotenv — explicitly do NOT install pycspr (it is Deploy-only and unfit per RESEARCH; installing it is the Pitfall-1 dead-end). PowerShell: `py -m venv agent/.venv`; then `agent/.venv/Scripts/python.exe -m pip install httpx python-dotenv`. Confirm both import.

    Write `agent/.env.example` (committed; the real .env is gitignored) filled with the LIVE values from RESEARCH Δ4: CASPER_NODE_RPC=https://node.testnet.casper.network/rpc, CASPER_NETWORK_NAME=casper-test, AGENT_SECRET_KEY_PATH=./secrets/secret_key.pem, plus reference-only lines CASPER_FAUCET_URL=https://testnet.cspr.live/tools/faucet and CASPER_EXPLORER_URL=https://testnet.cspr.live, and downstream EMPTY placeholders ANTHROPIC_API_KEY= and PINATA_JWT= (consumed in Phase 3; placeholders only, no real values). Do NOT create a committed real `.env`. Confirm with git check-ignore that a real `.env` would be ignored while `.env.example` is tracked.
  </action>
  <verify>
    <automated>test -f agent/pyproject.toml && test -f agent/.env.example && test -x agent/.venv/Scripts/python.exe && agent/.venv/Scripts/python.exe -c "import httpx, dotenv; print('deps ok')" && agent/.venv/Scripts/python.exe -c "import importlib.util,sys; sys.exit(0 if importlib.util.find_spec('pycspr') is None else 1)" && grep -q "node.testnet.casper.network/rpc" agent/.env.example && grep -q "casper-test" agent/.env.example</automated>
  </verify>
  <done>agent/.venv runs Python 3.14 with httpx + python-dotenv importable and pycspr absent; agent/.env.example holds the live RPC, network, secret-key path, faucet/explorer reference URLs, and empty ANTHROPIC/PINATA placeholders; no real .env is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Build the reusable chain.py wrapper around casper-client v5 + confirm query_balance shape (TOOL-02, open-risk #4)</name>
  <files>agent/src/steward/chain.py</files>
  <action>
    Implement `agent/src/steward/chain.py` — THE reusable chain layer that Phases 3-4 import (D-04). Load CASPER_NODE_RPC, CASPER_NETWORK_NAME, AGENT_SECRET_KEY_PATH from the environment (python-dotenv). Expose exactly these callables:

    - get_balance(public_key_hex: str) -> int : balance in motes. Resolve RESEARCH open-risk #4 first — make ONE live `query_balance` JSON-RPC POST (httpx) against the testnet RPC trying purse-identifier shape {"main_purse_under_public_key": "<hex>"}; if the node rejects that key shape, try {"main_purse_under_account_hash": "<account-hash>"} and record which shape the live node accepted. Use the accepted shape in the implementation. (An unfunded key may return a not-found/zero balance — that is still a successful shape confirmation; the funded read happens in 01-02.)
    - transfer(target_public_key_hex: str, amount_motes: int) -> str : shell out to casper-client put-txn transfer via subprocess using the EXACT flags recorded in CLI-FLAGS.md (Task 1) — chain-name from CASPER_NETWORK_NAME, secret-key from AGENT_SECRET_KEY_PATH, node from CASPER_NODE_RPC — request JSON output and return the transaction hash read from the JSON key CLI-FLAGS.md identified. Do NOT hard-code flag strings from memory or docs (Pitfall 2).
    - confirm(txn_hash: str, timeout_s: int = 180) -> bool : poll casper-client get-transaction <hash> (JSON) until an execution_result is present, returning True on Success and False on failure, with a bounded timeout.
    - delegate(validator_public_key_hex: str, amount_motes: int) -> str : Phase 4 stub — same binary/subprocess shape as transfer but using put-txn delegate flags from CLI-FLAGS.md. Raise NotImplementedError with a one-line note that it is wired in Phase 4 (STAK-01); keep the verified flag list in a comment so Phase 4 reuses it.

    Keep a single private subprocess helper for CLI calls. The module must import cleanly with the venv interpreter even before funding.
  </action>
  <verify>
    <automated>agent/.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'agent/src'); from steward import chain; assert all(hasattr(chain,f) for f in ('get_balance','transfer','confirm','delegate')); print('chain api ok')"</automated>
  </verify>
  <done>chain.py imports cleanly and exposes get_balance / transfer / confirm / delegate; query_balance purse-identifier shape is confirmed against a live RPC call (open-risk #4 resolved); transfer/confirm use the exact flags + tx-hash JSON key from CLI-FLAGS.md; delegate is a documented Phase-4 stub.</done>
</task>

<task type="auto">
  <name>Task 4: Generate the agent ed25519 keypair into ./secrets/ and record public key + account hash in deployments/testnet.json (TOOL-03 part a)</name>
  <files>secrets/, deployments/testnet.json</files>
  <action>
    Generate the agent's testnet keypair with `casper-client keygen secrets/` (ed25519 default — public_key_hex starts "01"). This writes secrets/secret_key.pem, secrets/public_key.pem, secrets/public_key_hex into the gitignored ./secrets/ directory (D-07). Derive the account hash with `casper-client account-address --public-key secrets/public_key_hex` (do not hand-roll the blake2b derivation — Don't-Hand-Roll).

    Create deployments/testnet.json recording: network "casper-test", rpc the live RPC URL, agent.public_key_hex (read from secrets/public_key_hex), agent.account_hash (from account-address), and an empty validators array (filled in Phase 4) plus an empty contracts object (filled in Phase 2). This file is TRACKED (judges verify the agent key on the explorer per SUBM-03) — it contains only the PUBLIC key + account hash, never the secret key. Verify git would ignore secrets/ but track deployments/testnet.json, and that no *.pem or public_key_hex is staged.
  </action>
  <verify>
    <automated>test -f secrets/secret_key.pem && test -f secrets/public_key_hex && test -f deployments/testnet.json && agent/.venv/Scripts/python.exe -c "import json; d=json.load(open('deployments/testnet.json')); assert d['agent']['public_key_hex'].strip() and d['agent']['account_hash'].strip(); print('keys recorded')" && git check-ignore secrets/secret_key.pem >/dev/null && ! git check-ignore deployments/testnet.json >/dev/null</automated>
  </verify>
  <done>secrets/ holds the ed25519 keypair (gitignored); deployments/testnet.json records the agent public_key_hex + account_hash (tracked, no secret material); the same public_key_hex is what 01-02 funds and proves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local FS -> git | Agent secret key (secrets/*.pem) and any real .env must never cross into the tracked repo (D-07). |
| Python -> casper-client subprocess | Flag strings + the secret-key path are passed to an external binary that signs transactions. |
| agent -> testnet RPC | Outbound JSON-RPC reads/writes to a public node over HTTPS; no inbound surface in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | secrets/secret_key.pem, real .env | high | mitigate | .gitignore already covers .env / secrets/ / *.pem; Task 2 + Task 4 verify with git check-ignore that the secret key and a real .env are ignored while only .env.example and deployments/testnet.json (public key + account hash only) are tracked. |
| T-01-02 | Spoofing/Tampering | wrong network (mainnet) | high | mitigate | CASPER_NETWORK_NAME hard-pinned to casper-test in .env.example and read by chain.py; D-01 testnet-only enforced — no mainnet RPC or chain-name appears anywhere. |
| T-01-03 | Tampering | guessed/stale CLI flags signing a malformed txn | medium | mitigate | chain.py copies flags only from CLI-FLAGS.md captured from the installed binary's --help (open-risk #1), never from memory/docs (Pitfall 2). |
| T-01-SC | Tampering | cargo/pip installs (casper-client, httpx, python-dotenv) | high | mitigate | RESEARCH Package Legitimacy Audit verdicts all OK (first-party casper-ecosystem; httpx/python-dotenv mature). No [ASSUMED]/[SUS]/[SLOP] packages requiring a blocking checkpoint; pycspr deliberately NOT installed. |
</threat_model>

<verification>
- casper-client --version prints 5.x; CLI-FLAGS.md captured from the installed binary (open-risk #1 resolved).
- agent/.venv imports httpx + python-dotenv; pycspr is NOT installed.
- chain.py imports and exposes get_balance / transfer / confirm / delegate; query_balance shape confirmed live (open-risk #4 resolved).
- secrets/ keypair generated (gitignored); deployments/testnet.json records public_key_hex + account_hash (tracked, no secret material).
- git check-ignore confirms no secret key / real .env is trackable.
</verification>

<success_criteria>
- TOOL-01: live Casper 2.0 toolchain verified against the installed casper-client v5 binary; real put-txn transfer/delegate flags + tx-hash JSON key recorded in CLI-FLAGS.md.
- TOOL-02 (setup): runnable agent/ package with the reusable chain.py wrapper (get_balance/transfer/confirm/delegate) on the chosen chain layer (casper-client v5 subprocess, D-03 #1 / D-04).
- TOOL-03 (part a): agent ed25519 testnet keypair generated and its public key + account hash recorded in deployments/testnet.json (funding + explorer confirmation completed in 01-02).
</success_criteria>

<output>
Create `.planning/phases/01-toolchain-reality-check/01-01-SUMMARY.md` when done. Record: installed casper-client version, the exact put-txn transfer/delegate flags + tx-hash JSON key, the confirmed query_balance purse-identifier shape, and the agent public_key_hex + account_hash — so 01-02 and Phases 3-4 reuse them.
</output>
