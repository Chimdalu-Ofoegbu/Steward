---
phase: 01-toolchain-reality-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/sidecar/package.json
  - agent/sidecar/chain.mjs
  - agent/sidecar/node_modules/
  - agent/pyproject.toml
  - agent/.env.example
  - agent/src/steward/__init__.py
  - agent/src/steward/chain.py
  - agent/.venv/
  - secrets/
  - deployments/testnet.json
  - .planning/phases/01-toolchain-reality-check/SIDECAR-API.md
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
must_haves:
  truths:
    - "agent/sidecar runs casper-js-sdk v5 on Node (npm install succeeds on Windows, no native build failure)"
    - "the sidecar exposes keygen / balance / transfer / delegate verbs returning JSON on stdout, using the casper-js-sdk v5 native builders (NativeTransferBuilder/NativeDelegateBuilder) + RpcClient"
    - "agent/ is a runnable Python package: .venv built via py launcher, httpx + python-dotenv installed, pycspr explicitly NOT installed"
    - "chain.py exposes get_balance / transfer / confirm + a delegate stub by shelling out to the Node sidecar, importable without error"
    - "a fresh ed25519 keypair exists in gitignored ./secrets/, and its public_key_hex + account hash are recorded in deployments/testnet.json"
  artifacts:
    - "agent/sidecar/chain.mjs (casper-js-sdk v5 wrapper) + package.json"
    - "agent/pyproject.toml, agent/.env.example"
    - "agent/src/steward/chain.py"
    - "secrets/secret_key.pem (gitignored) + public_key_hex"
    - "deployments/testnet.json"
    - ".planning/phases/01-toolchain-reality-check/SIDECAR-API.md"
  key_links:
    - "chain.py subprocess verbs === the verbs implemented in agent/sidecar/chain.mjs"
    - "AGENT_SECRET_KEY_PATH in .env.example -> ./secrets/secret_key.pem produced by the sidecar keygen"
    - "deployments/testnet.json public_key_hex === secrets/public_key_hex (same account funded in 01-02)"
---

<objective>
Stand up the agent's chain layer against the live Casper 2.0 "Condor" testnet using the **Node `casper-js-sdk` v5 sidecar** (per 01-RESEARCH.md POST-RESEARCH UPDATE: pycspr is Deploy-only and the `casper-client` Rust CLI does not compile on Windows / has no prebuilt binary; casper-js-sdk v5 is verified to install + load on this Windows/Node v24 box with native Casper-2.0 transaction builders). The Python brain shells out to the sidecar via subprocess, so Phases 3-4 reuse one `chain.py` unchanged (D-04) and Phase 5's frontend shares the same `casper-js-sdk` library.

Build: the `agent/sidecar/` Node project (casper-js-sdk v5) exposing keygen/balance/transfer/delegate; the `agent/` Python package (py launcher 3.14, httpx + python-dotenv, explicitly NOT pycspr); the reusable `chain.py` wrapper; and the agent's recorded testnet keypair. Funding + the acceptance probe happen in 01-02 (after the manual faucet gate).
</objective>

<execution_context>
@C:/Users/Ben/Desktop/B3NSAG3/Hackathons/Steward/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Ben/Desktop/B3NSAG3/Hackathons/Steward/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-toolchain-reality-check/01-RESEARCH.md
@.planning/phases/01-toolchain-reality-check/01-CONTEXT.md

# Environment: Windows 11. Python via the `py` launcher -> 3.14.2. Node v24.15 + npm 11.12 present.
# casper-client (Rust CLI) was tried and REJECTED — casper-types v5 uses Unix-only OpenOptions::mode() and won't build on Windows; no prebuilt Windows binary exists. Use the casper-js-sdk Node sidecar instead.
# .gitignore already covers: .env, secrets/, *.pem, node_modules/, __pycache__/, target/ — do NOT commit any secret key, .env, or node_modules.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Node casper-js-sdk v5 sidecar (keygen/balance/transfer/delegate) + document its API (TOOL-01)</name>
  <files>agent/sidecar/package.json, agent/sidecar/chain.mjs, .planning/phases/01-toolchain-reality-check/SIDECAR-API.md</files>
  <action>
    Create `agent/sidecar/` as a small Node ESM project: `npm init -y`, set `"type":"module"`, `npm install casper-js-sdk@latest` (verified 5.0.12, installs clean on Windows). Write `agent/sidecar/chain.mjs` — a CLI dispatcher (`node chain.mjs <verb> --json-args '{...}'` or argv flags) that prints a single JSON object to stdout and exits non-zero on error. Implement verbs using casper-js-sdk v5:
      - `keygen <dir>` : generate an ed25519 PrivateKey (KeyAlgorithm.ED25519), write secret_key.pem / public_key.pem / public_key_hex into <dir>; print {public_key_hex, account_hash}.
      - `account-hash <public_key_hex>` : derive + print the account hash.
      - `balance <public_key_hex>` : RpcClient(HttpHandler(RPC)).queryBalance(PurseIdentifier from public key) -> print {motes}. Confirm the exact PurseIdentifier construction for a public key against the v5 API (resolves RESEARCH open-risk #4). An unfunded key may read 0 / not-found — still a valid shape confirmation.
      - `transfer <target_hex> <motes>` : NativeTransferBuilder -> .from(senderPublicKey).target(targetPublicKey).amount(motes).chainName(network).payment(...) -> build, sign with the secret key, submit via RpcClient.putTransaction -> print {transaction_hash}.
      - `confirm <transaction_hash>` : poll RpcClient.getTransaction until execution_result present -> print {success, ...}.
      - `delegate <validator_hex> <motes>` : NativeDelegateBuilder (.validator/.amount/.delegator) — implement the build/sign/submit (Phase 4 will call it; verifying the builder + flags now de-risks STAK-01).
    Read env (CASPER_NODE_RPC, CASPER_NETWORK_NAME, AGENT_SECRET_KEY_PATH) via process.env (Python passes them through). Record the exact casper-js-sdk v5 call shapes you used (builder method chain, PurseIdentifier shape, putTransaction/getTransaction result JSON keys) into SIDECAR-API.md — the single source of truth Phases 3-4 reuse (tracked .planning artifact).
  </action>
  <verify>
    <automated>cd agent/sidecar && node -e "import('casper-js-sdk').then(s=>{if(!s.NativeTransferBuilder||!s.NativeDelegateBuilder||!s.RpcClient)process.exit(1);console.log('sdk ok')})" && node chain.mjs --help 2>&1 | grep -Eqi "keygen|balance|transfer" ; test -f ../../.planning/phases/01-toolchain-reality-check/SIDECAR-API.md</automated>
  </verify>
  <done>agent/sidecar installs casper-js-sdk v5 and chain.mjs dispatches keygen/balance/transfer/delegate/confirm using the v5 native builders; SIDECAR-API.md records the exact call shapes + the JSON key holding the transaction hash, sourced from the working SDK.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the agent/ Python package (py launcher venv, httpx + python-dotenv, NOT pycspr) + .env.example (TOOL-02 setup)</name>
  <files>agent/pyproject.toml, agent/.env.example, agent/src/steward/__init__.py, agent/.venv/</files>
  <action>
    Create the `agent/` package. Write `agent/pyproject.toml` (package name `steward`, `requires-python = ">=3.12"`, deps `httpx` + `python-dotenv`, src layout at `agent/src/steward`). Create `agent/src/steward/__init__.py`.
    Build the venv with the Windows `py` launcher and install ONLY httpx + python-dotenv — explicitly NOT pycspr (Deploy-only; Pitfall-1 dead-end). `py -m venv agent/.venv`; `agent/.venv/Scripts/python.exe -m pip install httpx python-dotenv`. Confirm both import.
    Write `agent/.env.example` (committed; real .env gitignored) with LIVE values: CASPER_NODE_RPC=https://node.testnet.casper.network/rpc, CASPER_NETWORK_NAME=casper-test, AGENT_SECRET_KEY_PATH=./secrets/secret_key.pem, STEWARD_SIDECAR=agent/sidecar/chain.mjs, reference lines CASPER_FAUCET_URL=https://testnet.cspr.live/tools/faucet, CASPER_EXPLORER_URL=https://testnet.cspr.live, and EMPTY placeholders ANTHROPIC_API_KEY=, STEWARD_MODEL=claude-opus-4-8, PINATA_JWT=, PINATA_GATEWAY=https://gateway.pinata.cloud/ipfs (consumed in Phase 3). No real .env committed.
  </action>
  <verify>
    <automated>test -f agent/pyproject.toml && test -f agent/.env.example && test -x agent/.venv/Scripts/python.exe && agent/.venv/Scripts/python.exe -c "import httpx, dotenv; print('deps ok')" && agent/.venv/Scripts/python.exe -c "import importlib.util,sys; sys.exit(0 if importlib.util.find_spec('pycspr') is None else 1)" && grep -q "node.testnet.casper.network/rpc" agent/.env.example && grep -q "casper-test" agent/.env.example</automated>
  </verify>
  <done>agent/.venv runs Python 3.14 with httpx + python-dotenv importable and pycspr absent; agent/.env.example holds the live RPC/network/secret-key/sidecar paths + empty ANTHROPIC/PINATA placeholders; no real .env committed.</done>
</task>

<task type="auto">
  <name>Task 3: Build the reusable chain.py wrapper around the Node sidecar (TOOL-02)</name>
  <files>agent/src/steward/chain.py</files>
  <action>
    Implement `agent/src/steward/chain.py` — THE reusable chain layer Phases 3-4 import (D-04). Load CASPER_NODE_RPC, CASPER_NETWORK_NAME, AGENT_SECRET_KEY_PATH, STEWARD_SIDECAR from env (python-dotenv). A single private helper runs `node <STEWARD_SIDECAR> <verb> <args>` via subprocess (passing env through), parses the JSON stdout, and raises on non-zero exit. Expose:
      - get_balance(public_key_hex) -> int (motes), via the sidecar `balance` verb.
      - transfer(target_public_key_hex, amount_motes) -> str (transaction_hash), via `transfer`.
      - confirm(txn_hash, timeout_s=180) -> bool, via `confirm` (bounded poll).
      - delegate(validator_public_key_hex, amount_motes) -> str : Phase-4 entry; may call the sidecar `delegate` verb (already implemented) or raise NotImplementedError with a one-line Phase-4 note — keep it wired so STAK-01 is a small step.
    The module must import cleanly with the venv interpreter before any funding.
  </action>
  <verify>
    <automated>agent/.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'agent/src'); from steward import chain; assert all(hasattr(chain,f) for f in ('get_balance','transfer','confirm','delegate')); print('chain api ok')"</automated>
  </verify>
  <done>chain.py imports cleanly and exposes get_balance / transfer / confirm / delegate, each backed by the Node sidecar subprocess; the same wrapper is reused unchanged in Phases 3-4.</done>
</task>

<task type="auto">
  <name>Task 4: Generate the agent ed25519 keypair into ./secrets/ and record public key + account hash in deployments/testnet.json (TOOL-03 part a)</name>
  <files>secrets/, deployments/testnet.json</files>
  <action>
    Generate the agent's testnet keypair via the sidecar: `node agent/sidecar/chain.mjs keygen secrets/` (ed25519 — public_key_hex starts "01"). This writes secrets/secret_key.pem, secrets/public_key.pem, secrets/public_key_hex into gitignored ./secrets/ (D-07), and prints the account hash. (Cross-check the account hash via the sidecar `account-hash` verb.)
    Create deployments/testnet.json recording: network "casper-test", rpc the live RPC URL, agent.public_key_hex (from secrets/public_key_hex), agent.account_hash, an empty validators array (Phase 4) and empty contracts object (Phase 2). TRACKED (judges verify the agent key on the explorer per SUBM-03) — PUBLIC key + account hash only, never the secret key. Verify git ignores secrets/ but tracks deployments/testnet.json, and no *.pem / public_key_hex is staged.
  </action>
  <verify>
    <automated>test -f secrets/secret_key.pem && test -f secrets/public_key_hex && test -f deployments/testnet.json && agent/.venv/Scripts/python.exe -c "import json; d=json.load(open('deployments/testnet.json')); assert d['agent']['public_key_hex'].strip() and d['agent']['account_hash'].strip(); print('keys recorded')" && git check-ignore secrets/secret_key.pem >/dev/null && ! git check-ignore deployments/testnet.json >/dev/null</automated>
  </verify>
  <done>secrets/ holds the ed25519 keypair (gitignored); deployments/testnet.json records the agent public_key_hex + account_hash (tracked, no secret material); the same public_key_hex is what 01-02 funds and proves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local FS -> git | Agent secret key (secrets/*.pem) and any real .env must never cross into the tracked repo (D-07). node_modules also gitignored. |
| Python -> Node sidecar subprocess | Verbs + the secret-key path are passed to the casper-js-sdk sidecar that signs transactions. |
| agent -> testnet RPC | Outbound JSON-RPC reads/writes to a public node over HTTPS; no inbound surface this phase. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | secrets/secret_key.pem, real .env | high | mitigate | .gitignore covers .env / secrets/ / *.pem / node_modules; Tasks 2 + 4 verify with git check-ignore that the secret key + real .env are ignored while only .env.example + deployments/testnet.json (public key + account hash) are tracked. |
| T-01-02 | Spoofing/Tampering | wrong network (mainnet) | high | mitigate | CASPER_NETWORK_NAME hard-pinned to casper-test in .env.example, read by chain.py + passed to the sidecar; D-01 testnet-only — no mainnet RPC/chain-name anywhere. |
| T-01-SC | Tampering | npm/pip installs (casper-js-sdk, httpx, python-dotenv) | high | mitigate | casper-js-sdk is first-party casper-ecosystem (RESEARCH audit OK); installed clean (0 vulns). pycspr deliberately NOT installed. |
</threat_model>

<verification>
- agent/sidecar installs casper-js-sdk v5 and chain.mjs dispatches keygen/balance/transfer/delegate/confirm; SIDECAR-API.md captured.
- agent/.venv imports httpx + python-dotenv; pycspr NOT installed.
- chain.py imports and exposes get_balance / transfer / confirm / delegate (backed by the sidecar).
- secrets/ keypair generated (gitignored); deployments/testnet.json records public_key_hex + account_hash (tracked, no secret material).
- git check-ignore confirms no secret key / real .env / node_modules is trackable.
</verification>

<success_criteria>
- TOOL-01: live Casper 2.0 toolchain verified — casper-js-sdk v5 sidecar builds Casper-2.0 native transactions; the exact builder/RPC call shapes + tx-hash JSON key recorded in SIDECAR-API.md.
- TOOL-02 (setup): runnable agent/ package with the reusable chain.py wrapper (get_balance/transfer/confirm/delegate) on the chosen chain layer (Node casper-js-sdk sidecar; D-04).
- TOOL-03 (part a): agent ed25519 testnet keypair generated and its public key + account hash recorded in deployments/testnet.json (funding + explorer confirmation in 01-02).
</success_criteria>

<output>
Create `.planning/phases/01-toolchain-reality-check/01-01-SUMMARY.md` when done. Record: casper-js-sdk version, the exact transfer/delegate builder call shapes + tx-hash JSON key, the confirmed queryBalance/PurseIdentifier shape, and the agent public_key_hex + account_hash — so 01-02 and Phases 3-4 reuse them.
</output>
