---
phase: 01-toolchain-reality-check
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - agent/probe.py
  - deployments/testnet.json
  - .planning/phases/01-toolchain-reality-check/REALITY-REPORT.md
autonomous: false
requirements: [TOOL-02, TOOL-03, TOOL-04]
user_setup:
  - service: casper-testnet-faucet
    why: "Fund the agent testnet key — the faucet is Casper-Wallet-gated, once-per-account, with no documented public HTTP API (D-08 / RESEARCH Pitfall 3). The user must fund the surfaced public key in a browser before the probe can run."
    dashboard_config:
      - task: "Connect Casper Wallet and request testnet CSPR for the agent public key shown by the checkpoint"
        location: "https://testnet.cspr.live/tools/faucet"
must_haves:
  truths:
    - "the agent public key is funded with testnet CSPR (manual faucet step) and the balance is read back > 0 via query_balance before the probe proceeds"
    - "probe.py reads the testnet balance, submits a tiny self-transfer, confirms it Succeeded on-chain, and prints the explorer transaction link"
    - "the funded account + the executed self-transfer are confirmable on testnet.cspr.live (TOOL-03 explorer confirmation)"
    - "a written REALITY-REPORT.md records the chain-layer decision + every delta vs the BUILD-PROMPT (TOOL-04)"
  artifacts:
    - "agent/probe.py"
    - ".planning/phases/01-toolchain-reality-check/REALITY-REPORT.md"
    - "deployments/testnet.json (funded balance + first txn hash recorded)"
  key_links:
    - "faucet-funded public_key_hex === deployments/testnet.json agent.public_key_hex (from 01-01)"
    - "probe.py imports steward.chain (01-01) — exercises the SAME submission path Phase 4 delegates with (D-04)"
    - "explorer link printed by probe === https://testnet.cspr.live/transaction/<txn_hash>"
---

<objective>
Prove the chain layer end-to-end against live testnet and record the reality report. This plan crosses the one step that genuinely cannot be automated — faucet funding (D-08 / RESEARCH Pitfall 3: the faucet is Casper-Wallet-gated with no public HTTP API) — as an explicit human checkpoint, then runs the acceptance probe (read balance -> self-transfer -> confirm -> explorer link) through the `chain.py` built in 01-01, and writes the deltas-vs-BUILD-PROMPT report the product owner requires.

This is the Phase-1 acceptance bar (TOOL-02/03) and the written decision (TOOL-04). It does NOT re-research and does NOT fund programmatically (no such API exists) — it surfaces the public key + faucet URL to the user, waits, then verifies on-chain.

Purpose: Close the non-negotiable reality-check gate with live, explorer-confirmable evidence using the exact submission path Phases 3-4 reuse (D-04).
Output: A funded agent account, a confirmed self-transfer visible on the explorer, agent/probe.py as the repeatable acceptance script, and REALITY-REPORT.md capturing the chain-layer decision + deltas.
</objective>

<execution_context>
@/tmp/steward/.claude/gsd-core/workflows/execute-plan.md
@/tmp/steward/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-toolchain-reality-check/01-RESEARCH.md
@.planning/phases/01-toolchain-reality-check/01-CONTEXT.md
@.planning/phases/01-toolchain-reality-check/CLI-FLAGS.md
@agent/src/steward/chain.py
@deployments/testnet.json

# Depends on 01-01: casper-client v5 installed, chain.py built, keypair in ./secrets/, public key + account hash in deployments/testnet.json.
# Faucet is wallet-gated/manual (D-08) — model it as a human-action checkpoint, NOT a programmatic funding task.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: MANUAL faucet funding — surface the agent public key + faucet URL, user funds, then verify balance (TOOL-03, D-08)</name>
  <files>deployments/testnet.json (read), secrets/public_key_hex (read)</files>
  <action>
    This is a blocking human-action checkpoint, not automatable work (D-08 / RESEARCH Pitfall 3 / open-risk #2). Programmatic funding was already determined impossible — the faucet is Casper-Wallet-gated with no public HTTP API. The executor's job here is to surface, wait, then verify — it MUST NOT pretend to fund programmatically. Steps: (1) print the agent public_key_hex (from secrets/public_key_hex / deployments/testnet.json) and the faucet URL https://testnet.cspr.live/tools/faucet; (2) pause for the user to fund the key in a browser and reply "funded"; (3) on resume, call chain.get_balance(public_key_hex) and require balance > 0 before continuing — re-poll / re-prompt if still 0, never proceed on an unfunded account. Only the PUBLIC key is surfaced; the secret key is never printed.
  </action>
  <what-built>
    01-01 generated the agent ed25519 keypair and recorded its public_key_hex in deployments/testnet.json. The faucet has NO automatable path: testnet.cspr.live/tools/faucet is gated behind a Casper Wallet browser login and is once-per-account (RESEARCH Pitfall 3 / Δ5 / open-risk #2). Per D-08, programmatic funding was attempted and is not possible — this step is human-only.
  </what-built>
  <how-to-verify>
    1. The executor prints the agent public_key_hex (from secrets/public_key_hex / deployments/testnet.json) and the faucet URL: https://testnet.cspr.live/tools/faucet
    2. You open the faucet in a browser, connect Casper Wallet, paste the agent public_key_hex, and request testnet CSPR.
    3. Wait for the faucet transaction to confirm (a few blocks).
    4. Reply "funded" (or paste the faucet txn hash). The executor then reads the balance back via chain.get_balance(public_key_hex) and requires it to be > 0 before continuing. If still 0, it re-polls / asks you to retry — it does NOT proceed on an unfunded account.
  </how-to-verify>
  <resume-signal>Type "funded" once the faucet transfer is confirmed (or paste the faucet txn hash); reply "skip" only to abort the phase.</resume-signal>
  <verify>
    <human-check>After the user replies "funded", chain.get_balance(public_key_hex) returns > 0 motes for the agent account; the executor confirms a non-zero balance before unblocking Task 2.</human-check>
  </verify>
  <done>The agent public_key_hex is funded via the manual faucet (D-08) and get_balance(public_key_hex) reads back > 0 before the probe runs; the secret key was never exposed.</done>
</task>

<task type="auto">
  <name>Task 2: Build agent/probe.py — read balance, submit + confirm a tiny self-transfer, print explorer link (TOOL-02 / TOOL-03 acceptance)</name>
  <files>agent/probe.py, deployments/testnet.json</files>
  <action>
    Write agent/probe.py — the repeatable Phase-1 acceptance script — importing the chain layer from 01-01 (steward.chain), never touching casper-client directly (chain.py is the only CLI seam). The script, run with the venv interpreter against the funded account from Task 1:

    1. READ: print get_balance(public_key_hex) in motes and CSPR (1 CSPR = 1e9 motes); require balance > 0 (fail loudly with the faucet URL if not — do not attempt a transfer on an empty account).
    2. SUBMIT: call transfer(target=own public_key_hex, amount_motes=2_500_000_000) (a 2.5 CSPR self-transfer per RESEARCH Step 4) and capture the returned transaction hash.
    3. CONFIRM: call confirm(txn_hash) and require it returns True (execution Success) — this is the "submits + confirms" bar, and exercises the Pitfall-4 confirm-before-read habit.
    4. EXPLORER: print the explorer transaction link https://testnet.cspr.live/transaction/<txn_hash> and the account link https://testnet.cspr.live/account/<public_key_hex> so the run is independently verifiable (TOOL-03).
    5. Record the funded balance and the confirmed self-transfer txn hash into deployments/testnet.json (e.g. an agent.first_self_transfer_txn field) as evidence.

    Use python-dotenv to load config; read public_key_hex from secrets/public_key_hex (or deployments/testnet.json). This is the SAME submission path Phase 4's delegate uses (D-04) — keep it thin.
  </action>
  <verify>
    <human-check>
      Run: `agent/.venv/Scripts/python.exe agent/probe.py` against the funded account. It must print a balance > 0, submit a self-transfer, print the txn hash, report confirm()=Success, and print the testnet.cspr.live transaction + account links. Open the transaction link and confirm it shows Success on the explorer (TOOL-03). The verifier confirms the explorer page renders the executed self-transfer.
    </human-check>
  </verify>
  <done>probe.py reads balance > 0, submits + confirms a self-transfer (confirm()=True / explorer shows Success), prints the explorer transaction + account links, and records the txn hash in deployments/testnet.json — the TOOL-02/TOOL-03 acceptance bar is met live.</done>
</task>

<task type="auto">
  <name>Task 3: Write REALITY-REPORT.md — chain-layer decision + deltas vs BUILD-PROMPT (TOOL-04)</name>
  <files>.planning/phases/01-toolchain-reality-check/REALITY-REPORT.md</files>
  <action>
    Write the product-owner-facing "reality report" the BUILD-PROMPT requires (CONTEXT specifics #3, TOOL-04). Source the content from 01-RESEARCH.md (chain-layer recommendation + Deltas table) and the LIVE results captured in 01-01/01-02 — do NOT re-derive, surface what was proven:

    - DECISION: chain layer = casper-client v5 CLI driven from Python via subprocess (D-03 fallback #1), with the one-line rationale that pycspr 1.2.0 is Deploy-only and cannot do Casper 2.0 native-auction delegation; same binary/wrapper reused for Phase 4 delegate (D-04).
    - VERIFIED-LIVE: installed casper-client version, the confirmed put-txn transfer/delegate flags + tx-hash JSON key (from CLI-FLAGS.md), the confirmed query_balance purse-identifier shape (open-risk #4), the live RPC (https://node.testnet.casper.network/rpc, api 2.0.0 / casper-test), and the explorer-confirmed self-transfer txn hash from the probe.
    - DELTAS vs BUILD-PROMPT: carry over Δ1-Δ7 from RESEARCH, marking each as confirmed-at-execution (especially Δ1 pycspr switch, Δ4 live RPC, Δ5 manual faucet) and flag anything that differed from RESEARCH during execution.
    - OPEN-RISKS STATUS: state how open-risks #1 (CLI flags) and #4 (query_balance shape) were resolved, and that the faucet (#2) is confirmed-manual.

    Keep it concise and evidence-backed (txn hashes, versions, flag names) — this is the gate sign-off the owner reads before Phase 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-toolchain-reality-check/REALITY-REPORT.md && grep -Eqi "casper-client" .planning/phases/01-toolchain-reality-check/REALITY-REPORT.md && grep -Eqi "pycspr" .planning/phases/01-toolchain-reality-check/REALITY-REPORT.md && grep -Eqi "delta|Δ" .planning/phases/01-toolchain-reality-check/REALITY-REPORT.md</automated>
  </verify>
  <done>REALITY-REPORT.md records the locked chain-layer decision (casper-client v5 CLI subprocess), the live-verified versions/flags/RPC/txn-hash evidence, and the confirmed deltas vs the BUILD-PROMPT — the TOOL-04 written sign-off is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human/browser -> faucet | The agent public key is pasted into a third-party wallet-gated faucet; only the PUBLIC key leaves the machine. |
| probe.py -> chain.py -> casper-client | The signing path runs through the 01-01 wrapper using the secret key in ./secrets/. |
| agent -> testnet RPC/explorer | Outbound reads/writes over HTTPS to the public testnet node + explorer; testnet-only (D-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | faucet funding step | medium | mitigate | Only the PUBLIC key is surfaced to the user/faucet; the secret key never leaves ./secrets/ and is never printed. The checkpoint instructions paste public_key_hex only. |
| T-01-05 | Tampering | acting on an unfunded/unconfirmed account | medium | mitigate | probe.py requires balance > 0 before transfer and requires confirm()=Success before reporting success (Pitfall 4 confirm-before-read); it never proceeds on an empty/unconfirmed account. |
| T-01-06 | Spoofing | wrong network / wrong account funded | high | mitigate | chain.py is pinned to casper-test (D-01); the funded key === deployments/testnet.json agent.public_key_hex, cross-checked before the probe runs; explorer link lets the owner independently verify the account + txn. |
</threat_model>

<verification>
- Faucet checkpoint completed: get_balance(public_key_hex) > 0 before the probe runs.
- probe.py: balance read > 0, self-transfer submitted, confirm()=Success, explorer transaction + account links printed.
- Explorer (testnet.cspr.live) shows the funded account and the executed self-transfer (TOOL-03).
- deployments/testnet.json records the first self-transfer txn hash.
- REALITY-REPORT.md records the chain-layer decision + deltas vs BUILD-PROMPT (TOOL-04).
</verification>

<success_criteria>
- TOOL-02: agent/probe.py reads the agent CSPR balance from testnet AND submits + confirms a tiny self-transfer via the documented fallback (casper-client v5 CLI through chain.py) — the acceptance script runs green.
- TOOL-03: the agent testnet keypair is funded (manual faucet, D-08) and the funded account + self-transfer are confirmed on testnet.cspr.live.
- TOOL-04: REALITY-REPORT.md records the chain-layer library decision and flags every delta from the BUILD-PROMPT's assumptions before Phase 2 proceeds.
</success_criteria>

<output>
Create `.planning/phases/01-toolchain-reality-check/01-02-SUMMARY.md` when done. Record: the funded balance, the confirmed self-transfer txn hash + explorer link, and a pointer to REALITY-REPORT.md — closing the Phase-1 reality-check gate (TOOL-01..04).
</output>
